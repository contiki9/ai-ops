# AI Agents Guidelines

このドキュメントは、本リポジトリ（AI-Ops）を読み取って操作を行う AI アシスタント（エージェント）向けの共通ガイドラインです。

## 基本アプローチ
1. **コンテキストの理解**
   あなたがユーザーに代わってタスクを実行する際、必要に応じて `commands/` ディレクトリ配下にある特定のワークフローファイル（Markdown形式）を読み込み、そこに定義された手順とルールに従ってください。
2. **ツールの活用**
   利用可能なツール（検索、ファイル編集、コマンド実行など）を最適に組み合わせてタスクを遂行してください。
   - **GitHub CLI（`gh`）で Issue を読むとき**: デフォルトの人間向け表示は、リポジトリや `gh` のバージョンによって **Projects (classic)** の `projectCards` を問い合わせ、GraphQL の非推奨エラーで **`gh issue view` 全体が exit code 1 になる**ことがあります。自動化・エージェント作業では、必要フィールドだけを指定して取得する経路を優先してください。
     - 例: `gh issue view <番号> --json title,body,state,labels,url`（`--repo owner/repo` が必要な場合は付与）
     - 代替: `gh api` で REST 上に必要な情報だけ取得する
   - 人間がブラウザで開く場合は、環境によって `gh issue view <番号> -w` が使えます。
   - PR のメタデータ取得、`gh pr edit` が GraphQL で失敗する場合の回避、`gh issue edit` の本文の渡し方などは、必要に応じて `commands/github-cli-notes.md` を参照してください（挙動は `gh --version` ごとに変わり得るため、再現調査時はバージョン併記の余地を残します）。
3. **日本語でのコミュニケーションと成果物**
   ユーザーとの対話や、成果物（手順の説明、コピペ用の例、プルリクエストの説明、ドキュメント、Issue のタイトル・本文の案、確認用チェックリストなど）は、誤解や取り違えを減らすため、特に指示がない限り**日本語**で書いてください。コミットメッセージは、`commands/git-commit.md` の手順・例（conventional commits など）に従う場合は英語で構いません。コマンド名や CLI のフラグなど、ツールが要求する英語の表記はそのままで構いません。

## ワークフローの参照方法
ユーザーから特定のタスク（例：「PRのリファクタリングをして」「テストを追加して」）を依頼された場合、関連しそうな `commands/` 内のファイルを探し出し、その指示をベースラインとして活用することを検討してください。

## 新規コマンド・スキルの追加
あなたが新しいコマンドやスキルを本リポジトリに追加するよう指示された場合：
- Markdown 形式で目的・要件・出力形式を明確に定義してください。
- 適切なカテゴリ（ディレクトリ）に配置してください。
- 必要に応じて `README.md` のコマンド一覧も更新してください。
