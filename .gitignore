# Cursor Agent state (keep shared commands only)
.cursor/history/
.cursor/tmp/

# Keep the .cursor directory tracked
!.cursor/
!.cursor/**/*

# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json
yarn.lock

# Build outputs
dist/
build/
out/
*.tgz

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~

# OS artifacts
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs and caches
logs/
*.log
*.cache

# Working scratch (not committed): PR/Issue drafts, agent notes, etc.
.tmp/

# Stray temp-named files outside .tmp/ (prefer placing files under .tmp/ instead)
*.tmp
*.temp

# Cache directories
.cache/
.parcel-cache/
